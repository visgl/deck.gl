const PYODIDE_CDN_URL='https://cdn.jsdelivr.net/pyodide/v314.0.2/full/pyodide.mjs';const UPLOADS_DIR='/uploads';const knownModules=new Set(['pandas','pydeck']);const uploadedFiles={};let currentRunRequestId=null;let pendingStderr='';let pendingStdout='';let flushScheduled=false;let captureStreams=false;const pyodideReadyPromise=(async()=>{const{loadPyodide}=await import(PYODIDE_CDN_URL);const pyodide=await loadPyodide();pyodide.FS.mkdirTree(UPLOADS_DIR);pyodide.setStdout({raw(charCode){if(!captureStreams){return;}pendingStdout+=String.fromCharCode(charCode);scheduleFlush();}});pyodide.setStderr({raw(charCode){if(!captureStreams){return;}pendingStderr+=String.fromCharCode(charCode);scheduleFlush();}});pyodide.runPython("\nimport json\nuploaded_files = json.loads(\"{}\")\n  ");return pyodide;})();const runtimeReadyPromise=(async()=>{const pyodide=await pyodideReadyPromise;await pyodide.loadPackage(['micropip','pandas']);const micropip=pyodide.pyimport('micropip');await micropip.install('pydeck');})();function postResult(id,result,transfer){if(transfer===void 0){transfer=[];}self.postMessage({id,type:'result',result},transfer);}function postError(id,error){self.postMessage({id,type:'error',error});}function postUpdate(id,update){self.postMessage({id,type:'update',update});}function resetRunStreams(){pendingStdout='';pendingStderr='';flushScheduled=false;}function flushPendingOutput(){if(currentRunRequestId===null){resetRunStreams();return;}const update={};if(pendingStdout){update.stdout=pendingStdout;pendingStdout='';}if(pendingStderr){update.stderr=pendingStderr;pendingStderr='';}flushScheduled=false;if(update.stdout||update.stderr){postUpdate(currentRunRequestId,update);}}function scheduleFlush(){if(flushScheduled){return;}flushScheduled=true;queueMicrotask(()=>{flushPendingOutput();});}function getUniqueStoredName(fileName){const lastDot=fileName.lastIndexOf('.');const hasExtension=lastDot>0;const baseName=hasExtension?fileName.slice(0,lastDot):fileName;const extension=hasExtension?fileName.slice(lastDot):'';const safeBaseName=baseName.replace(/[^a-zA-Z0-9-_]+/g,'_')||'upload';const uniqueSuffix=Date.now()+"-"+Math.random().toString(36).slice(2,8);return safeBaseName+"__"+uniqueSuffix+extension;}async function syncUploadedFiles(pyodide){const uploadedFilePaths=Object.fromEntries(Object.entries(uploadedFiles).map(_ref=>{let[fileName,fileRecord]=_ref;return[fileName,fileRecord.path];}));pyodide.globals.set('__uploaded_files_json',JSON.stringify(uploadedFilePaths));pyodide.runPython("\nimport json\nuploaded_files = json.loads(__uploaded_files_json)\n  ");}function findImports(pyodide,code){pyodide.globals.set('__source_for_import_scan',code);const imports=pyodide.runPython("\nfrom pyodide.code import find_imports\n\nfind_imports(__source_for_import_scan)\n  ");return Array.isArray(imports)?imports:imports.toJs();}function getMimeType(path){const normalizedPath=path.toLowerCase();if(normalizedPath.endsWith('.html')||normalizedPath.endsWith('.htm')){return'text/html';}if(normalizedPath.endsWith('.json')){return'application/json';}if(normalizedPath.endsWith('.csv')){return'text/csv';}if(normalizedPath.endsWith('.txt')||normalizedPath.endsWith('.log')||normalizedPath.endsWith('.md')){return'text/plain';}if(normalizedPath.endsWith('.svg')){return'image/svg+xml';}if(normalizedPath.endsWith('.png')){return'image/png';}if(normalizedPath.endsWith('.jpg')||normalizedPath.endsWith('.jpeg')){return'image/jpeg';}if(normalizedPath.endsWith('.gif')){return'image/gif';}if(normalizedPath.endsWith('.webp')){return'image/webp';}return'application/octet-stream';}function getBasename(path){const segments=path.split('/');return segments[segments.length-1]||path;}function getMonitoredRoots(pyodide){const cwd=String(pyodide.runPython("\nimport os\nos.getcwd()\n  "));return Array.from(new Set([cwd,UPLOADS_DIR]));}function snapshotDirectory(pyodide,path,snapshot){let stat;try{stat=pyodide.FS.stat(path);}catch(_unused){return;}if(pyodide.FS.isFile(stat.mode)){snapshot.set(path,{mtime:Number(stat.mtime),size:Number(stat.size)});return;}if(!pyodide.FS.isDir(stat.mode)){return;}for(const entry of pyodide.FS.readdir(path)){if(entry==='.'||entry==='..'){continue;}const childPath=path==='/'?"/"+entry:path+"/"+entry;snapshotDirectory(pyodide,childPath,snapshot);}}function takeFilesystemSnapshot(pyodide){const snapshot=new Map();for(const root of getMonitoredRoots(pyodide)){snapshotDirectory(pyodide,root,snapshot);}return snapshot;}function getWrittenFiles(pyodide,before){const after=takeFilesystemSnapshot(pyodide);const files=[];const transfer=[];for(const[path,entry]of after.entries()){const previousEntry=before.get(path);const isChanged=!previousEntry||previousEntry.mtime!==entry.mtime||previousEntry.size!==entry.size;if(!isChanged){continue;}const bytes=pyodide.FS.readFile(path,{encoding:'binary'});const buffer=bytes.buffer.slice(bytes.byteOffset,bytes.byteOffset+bytes.byteLength);files.push({buffer,name:getBasename(path),type:getMimeType(path)});transfer.push(buffer);}return{files,transfer};}async function registerUploadedFile(pyodide,file){const previousRecord=uploadedFiles[file.name];const storedName=getUniqueStoredName(file.name);const path=UPLOADS_DIR+"/"+storedName;pyodide.FS.writeFile(path,new Uint8Array(file.buffer));if(previousRecord){try{pyodide.FS.unlink(previousRecord.path);}catch(_unused2){// Ignore stale file cleanup failures and keep the latest upload registered.
}}uploadedFiles[file.name]={originalName:file.name,path,storedName,size:file.size,type:file.type,uploadedAt:new Date().toISOString()};}self.onmessage=async event=>{const request=event.data;try{const pyodide=await pyodideReadyPromise;switch(request.type){case'prepare-runtime':await runtimeReadyPromise;postResult(request.id,true);return;case'get-version':{const result=await pyodide.runPython("\n            import sys\n            sys.version\n        ");postResult(request.id,String(result));return;}case'preload-files':{for(const file of request.files){var _response$headers$get;const response=await fetch(file.url);if(!response.ok){throw new Error("Failed to preload "+file.name+": "+response.status+" "+response.statusText);}const buffer=await response.arrayBuffer();await registerUploadedFile(pyodide,{buffer,name:file.name,size:buffer.byteLength,type:(_response$headers$get=response.headers.get('content-type'))!=null?_response$headers$get:''});}await syncUploadedFiles(pyodide);postResult(request.id,uploadedFiles);return;}case'upload-files':{for(const file of request.files){await registerUploadedFile(pyodide,file);}await syncUploadedFiles(pyodide);postResult(request.id,uploadedFiles);return;}case'run-python':{const startedAt=performance.now();currentRunRequestId=request.id;resetRunStreams();postUpdate(request.id,{status:'Loading packages...'});await runtimeReadyPromise;const importedModules=findImports(pyodide,request.code);const hasUnknownImports=importedModules.some(moduleName=>!knownModules.has(moduleName));if(hasUnknownImports){captureStreams=false;await pyodide.loadPackagesFromImports(request.code);importedModules.forEach(moduleName=>{knownModules.add(moduleName);});}postUpdate(request.id,{status:'Running...'});captureStreams=true;const filesystemBeforeRun=takeFilesystemSnapshot(pyodide);const result=await pyodide.runPython(request.code);flushPendingOutput();const writtenFiles=getWrittenFiles(pyodide,filesystemBeforeRun);const payload={files:writtenFiles.files,result:String(result),timeElapsed:performance.now()-startedAt};captureStreams=false;currentRunRequestId=null;postResult(request.id,payload,writtenFiles.transfer);return;}default:throw new Error('Unknown worker request');}}catch(error){captureStreams=false;flushPendingOutput();currentRunRequestId=null;postError(request.id,error instanceof Error?error.message:String(error));}};
