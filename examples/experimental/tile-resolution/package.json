{
  "name": "carto-deckgl-tile-resolution",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "vite",
    "start-local": "vite --config ../../vite.config.local.mjs",
    "build": "vite build --config ../../vite.config.local.mjs --outDir dist",
    "preview": "vite preview",
    "deploy": "vercel build --prod && vercel deploy --prebuilt --prod",
    "link-deck": "yarn link @deck.gl/core && yarn link @deck.gl/layers && yarn link @deck.gl/geo-layers && yarn link @deck.gl/mesh-layers && yarn link @deck.gl/aggregation-layers && yarn link @deck.gl/carto && yarn link @deck.gl/extensions",
    "unlink-deck": "yarn unlink @deck.gl/core && yarn link @deck.gl/layers && yarn link @deck.gl/geo-layers && yarn link @deck.gl/mesh-layers && yarn link @deck.gl/aggregation-layers && yarn link @deck.gl/carto && yarn link @deck.gl/extensions"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "dependencies": {
    "@deck.gl/aggregation-layers": "^9.0.0-beta.3",
    "@deck.gl/carto": "^9.0.0-beta.3",
    "@deck.gl/core": "^9.0.0-beta.3",
    "@deck.gl/extensions": "^9.0.0-beta.3",
    "@deck.gl/geo-layers": "^9.0.0-beta.3",
    "@deck.gl/layers": "^9.0.0-beta.3",
    "@deck.gl/mesh-layers": "^9.0.0-beta.3",
    "lil-gui": "^0.19.1",
    "maplibre-gl": "^3.5.2"
  }
}
